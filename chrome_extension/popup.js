async function saveJob() {
    // Get authentication data
    // Create new tab and redirect user there if they are not logged in

    // if user is logged in
        // make api call

        // if api call returns 200 OK, display a "job saved" message

        // else, return an error
}